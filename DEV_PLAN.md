# DEV_PLAN — порядок фаз, зависимости, блокеры

## Состояние

MVP закрыт (см. `DONE.md`). Ниже — план следующих фаз. Задачи берутся из
`TODO.md` сверху вниз внутри разблокированной фазы.

## Фаза 7 — стабилизация MVP — закрыта

T044 (обработчик исключений), T043 (тесты диспетчера), T041 (кэш `food_stats`)
и T040 (напоминание после еды) сделаны — см. `DONE.md`.

## Фаза 8 — расширение ввода — закрыта

T039 (штрихкод + Open Food Facts), T045 (рендер страниц скана в vision-путь) и
T038 (импорт CSV из LibreView / Clarity) сделаны — см. `DONE.md`.

## Фаза 9 — платформы здоровья — закрыта

T046: мост живёт в `apps/health-bridge/` (в этом же репозитории — инструкция,
сервер и приложение меняются вместе). T037: релей стал платформо-нейтральным
(`/health/sync`, старый путь навсегда алиасом), имена образцов Apple
приводятся к нашим видам, инструкция для iPhone — «Быстрые команды».

**[BLOCKED: нужен физический телефон для сквозной проверки]** — Android-мост
собирается в CI, но на устройстве не прогонялся; iOS-путь описан словами и
тоже не прогонялся вживую. Релей по-прежнему проверяется синтетическими
запросами (`tests/test_web.py`).

## Фаза 10 — продукт

| Задача | Зависит от | Примечание |
|---|---|---|
| T042 недельный дайджест | T041 | считать по кэшу, а не пересчётом |
| T047 PDF-отчёт для врача | T042 | переиспользовать графики из `charts/` |
| T048 мультиязычность | — | требует вынести все строки из `reporting.py` и хендлеров |

## Фаза 11 — web (`web/`)

| Задача | Зависит от | Примечание |
|---|---|---|
| T071 словарь: medication/symptom | — | ждёт `/meds`- и `WellbeingFlow`-аналогов в web |
| T072 иконки PWA | — | косметика, не блокирует деплой |

**[BLOCKED: T070 — нужен реальный браузер/устройство с push-сервисом для
сквозной проверки Web Push]** — `web/lib/webpush.php` собран по RFC 8291/8292
и самопроверен юнит-тестом (подпись/шифрование корректно расшифровываются
обратно), но живая доставка не прогонялась — тот же случай, что и мост
Samsung Health (фаза 9).

## Постоянные ограничения

- Принципы I–III конституции не обсуждаются ни в одной фазе.
- Любая новая модельная задача получает фикстуру в `src/llm/mock.py` в том же PR.
- Изменение `models.py` — вместе с миграцией Alembic в одном коммите.
