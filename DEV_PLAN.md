# DEV_PLAN — порядок фаз, зависимости, блокеры

## Состояние

MVP закрыт (см. `DONE.md`). Ниже — план следующих фаз. Задачи берутся из
`TODO.md` сверху вниз внутри разблокированной фазы.

## Фаза 7 — стабилизация MVP

**Цель:** пережить первых реальных пользователей без ручного вмешательства.

| Задача | Зависит от | Примечание |
|---|---|---|
| T044 обработчик исключений | — | делать первым: без него ошибки видны только в логах |
| T043 тесты диспетчера aiogram | T044 | тела обработчиков уже покрыты; нужны фильтры и middleware |
| T041 кэш `food_stats` | — | таблица уже есть; нужна инвалидация при новой экскурсии и при правке приёма пищи |
| T040 напоминание после еды | T044 | обязательно отключаемое; иначе бот превращается в спам |

## Фаза 8 — расширение ввода

| Задача | Зависит от | Примечание |
|---|---|---|
| T039 штрихкод + Open Food Facts | — | `pyzbar` требует системный `zbar` → правка Dockerfile |
| T045 OCR-fallback для сканов PDF | T039 | можно закрыть тем же vision-вызовом постранично |
| T038 импорт CSV из LibreView / Clarity | T041 | массовая вставка обрушит пересчёт `/stats` без кэша |

## Фаза 9 — платформы здоровья

| Задача | Зависит от | Примечание |
|---|---|---|
| T037 Apple HealthKit | — | переименование пути с сохранением обратной совместимости |

T046 закрыта: мост живёт в `apps/health-bridge/` (в этом же репозитории —
инструкция, сервер и приложение меняются вместе).

**[BLOCKED: нужен физический Android-телефон с Samsung Health для сквозной
проверки моста]** — код моста написан и собирается в CI, но на устройстве не
прогонялся; релей по-прежнему проверяется синтетическими запросами
(`tests/test_web.py`).

## Фаза 10 — продукт

| Задача | Зависит от | Примечание |
|---|---|---|
| T042 недельный дайджест | T041 | считать по кэшу, а не пересчётом |
| T047 PDF-отчёт для врача | T042 | переиспользовать графики из `charts/` |
| T048 мультиязычность | — | требует вынести все строки из `reporting.py` и хендлеров |

## Постоянные ограничения

- Принципы I–III конституции не обсуждаются ни в одной фазе.
- Любая новая модельная задача получает фикстуру в `src/llm/mock.py` в том же PR.
- Изменение `models.py` — вместе с миграцией Alembic в одном коммите.
