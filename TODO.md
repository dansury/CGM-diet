# TODO — незакрытые задачи

Только то, что **не** сделано. Завершённое переносится в `DONE.md` (см. `CLAUDE.md` #5).
Порядок фаз и зависимости — в `DEV_PLAN.md`.

## Приоритет 4 — продукт

- [ ] T047 Экспорт отчёта для врача в PDF (сводка + графики + метрики)
- [ ] T048 Мультиязычность (сейчас всё по-русски, строки захардкожены)

## Приоритет 5 — то, что открылось этой работой

- [ ] T072 Пометки зрения у живых моделей Яндекса ставятся по имени слага
      (`gemma-3-*`, `*-vl-*`, …) — `GET /v1/models` модальности не отдаёт.
      Проверить по документации провайдера, когда появится поле, и убрать
      эвристику из `ModelCatalog::yandexSeesImages`

- [ ] T065 Решить судьбу страниц `web/romanesco.html` и
      `web/romanesco-lines.html`: оставить пасхалкой по прямой ссылке или
      вынести на лендинг (сейчас на них ниоткуда нет ссылок)

- [ ] T049 Дополнить `config/drug_cids.json` до ~300 препаратов (сейчас 75);
      каждый CID сверять вручную — неверный показывает чужие побочки
- [ ] T050 Русские формулировки для побочек полного датасета: сейчас переведены
      42 частых термина, остальные показываются по-английски
- [ ] T051 Кнопка «поправить время» и правка голосом для карточки продукта
      (сейчас у продукта правятся только название и БЖУК)
- [ ] T069 Оценка сна по появлениям видит только чат с ботом: у молчаливого
      пользователя ночей не набирается. Возможный шаг — утренняя и вечерняя
      кнопка отметки, но её ещё нет
- [ ] T059 Дополнить `config/nutrient_foods.json`: сейчас 12 нутриентов, маркеры
      сопоставляются по подстроке — редкие названия из лабораторий не находятся
- [ ] T086 Гарвардской тарелки в web нет вообще — ни разбора после записи, ни
      настроек, ни свода за неделю. Перенос требует всего модуля целиком
      (категории, склейка сессий, режим питания), а не одной карточки
      (`spec/plate.md`, пометка `[TG-ONLY]`)

- [ ] T085 Сравнение по компонентам в недельной карточке web
      (`web/app/js/digest.js`): «после чего подъём стал выше» там нет — нужен
      весь движок экскурсий и статистики (`src/analytics/windows.py`,
      `stats.py`) вторым экземпляром на JavaScript, а он разойдётся с первым
      ровно там, где проекту это дороже всего. Либо считать это на сервере
      (`web/api/`), либо признать разрыв постоянным
      (`spec/web.md` § Неделя в сравнении)

- [ ] T083 Web не умеет распознавать упаковку с этикетки вообще (нет
      `kind="product"` в `web/app/js/dictionary.js`, нет фото-флоу вроде
      бот-`prod:eat`) — поэтому и памяти последней граммовки для продуктов
      там взяться неоткуда (`spec/dictionary.md` § Память последней
      граммовки, добавлена в этой сессии для бота). Паритет tg/web
      (`CLAUDE.md` #11) для полноты требует всей этикеточной ветки на web, а
      не только памяти веса — отдельная функциональность, не однострочный
      довесок к этой сессии

## Приоритет 6 — web (`web/`)

- [BLOCKED: нужен реальный браузер/устройство с настоящим push-сервисом —
  в среде сборки нет способа проверить доставку end-to-end] T070 Сквозная
  проверка Web Push (`web/lib/webpush.php`): VAPID JWT и aes128gcm
  самопроверены юнит-тестом (подпись/шифрование расшифровываются обратно
  корректно), но реальная доставка через push-сервис браузера не прогонялась
- [ ] T071 «Мои блюда» в web (`web/app/js/dictionary.js`) пока не поддерживают
      виды `medication`/`symptom` — только `meal`/`item`/`product`; в боте
      они есть (`spec/dictionary.md`). Паритет tg/web (`CLAUDE.md` #11)
      требует либо реализовать, либо это осознанный `[WEB-ONLY/TG-ONLY]`
      разрыв — сейчас просто временно не сделано
- [ ] T073 «Умное» время в web не видит нагрузку: тренировки записываются
      только в боте, в IndexedDB такого store нет, и шаблон с единственным
      сигналом `activity` остаётся на времени админа
      (`web/app/js/notify.js`, `spec/notifications.md`)
- [ ] T082 Напоминание «померьте сахар» через 60–75 мин после еды (T040) —
      только в боте: у web-уведомлений (`web/lib/notifications.php`) есть
      только расписание по времени суток, понятия «через N минут после
      события X» там нет — нужна отдельная инфраструктура (хранить время
      последней записи еды, отдельный проход `push_send.php` вне
      `notification_templates`). `[TG-ONLY]` до реализации,
      `spec/onboarding.md` § Сахарный трек
