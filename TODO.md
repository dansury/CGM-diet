# TODO — незакрытые задачи

Только то, что **не** сделано. Завершённое переносится в `DONE.md` (см. `CLAUDE.md` #5).
Порядок фаз и зависимости — в `DEV_PLAN.md`.

## Приоритет 1 — качество MVP

- [ ] T041 Кэш `food_stats` вместо пересчёта агрегатов на каждый `/stats`
      (таблица уже в схеме; инвалидация по новой экскурсии)
- [ ] T040 Напоминание «померить сахар» через 60–75 мин после записи еды,
      отключаемое в `/settings`
- [ ] T043 Тесты диспетчера aiogram: фильтры, middleware, порядок роутеров
      (тела обработчиков уже покрыты в `tests/test_handlers_flow.py`)

## Приоритет 2 — расширение ввода

- [ ] T039 Локальное чтение штрихкода (`pyzbar`) + Open Food Facts как источник
      состава, когда этикетка не читается
- [ ] T038 Импорт истории CGM из CSV: LibreView, Dexcom Clarity
- [ ] T045 OCR-fallback для PDF без текстового слоя (сейчас просим фото страницы)

## Приоритет 3 — интеграции

- [ ] T037 Apple HealthKit через тот же релей (`/health/samsung` → `/health/sync`,
      с обратной совместимостью пути)

## Приоритет 4 — продукт

- [ ] T042 Недельный дайджест: что изменилось в статистике за неделю
- [ ] T047 Экспорт отчёта для врача в PDF (сводка + графики + метрики)
- [ ] T048 Мультиязычность (сейчас всё по-русски, строки захардкожены)

## Приоритет 5 — то, что открылось этой работой

- [ ] T065 Решить судьбу страниц `web/romanesco.html` и
      `web/romanesco-lines.html`: оставить пасхалкой по прямой ссылке или
      вынести на лендинг (сейчас на них ниоткуда нет ссылок)

- [ ] T064 Использовать цели знакомства дальше первого экрана: недельный дайджест
      и `/today` под цель (сейчас цели влияют только на порядок вопросов анкеты,
      выбор первой подсказки и карточку `/body`)
- [ ] T049 Дополнить `config/drug_cids.json` до ~300 препаратов (сейчас 75);
      каждый CID сверять вручную — неверный показывает чужие побочки
- [ ] T050 Русские формулировки для побочек полного датасета: сейчас переведены
      42 частых термина, остальные показываются по-английски
- [ ] T051 Кнопка «поправить время» и правка голосом для карточки продукта
      (сейчас у продукта правятся только название и БЖУК)
- [ ] T052 Автообновление каталога свободных моделей по расписанию
      (сейчас — раз в сутки при обращении)
- [ ] T068 Учитывать длительность сна в дневном коридоре калорий: сейчас сон
      только сопоставляется с уже съеденным, на сам коридор не влияет
- [ ] T069 Оценка сна по появлениям видит только чат с ботом: у молчаливого
      пользователя ночей не набирается. Возможный шаг — утренняя и вечерняя
      кнопка отметки, но её ещё нет
- [ ] T053 Учитывать шаги из Samsung Health в дневном коридоре как отдельную
      строку (сейчас в расход идут только тренировки: шаги за сутки без
      калибровки дали бы двойной счёт с ходьбой, записанной руками)
- [ ] T054 Пересчитывать коридор калорий по факту динамики веса (сейчас план
      строится от формулы; через 4–6 недель замеров можно оценивать реальный
      TDEE по изменению веса и съеденному)
- [ ] T059 Дополнить `config/nutrient_foods.json`: сейчас 12 нутриентов, маркеры
      сопоставляются по подстроке — редкие названия из лабораторий не находятся
- [ ] T060 Тарелка считает только массу: напиток в 300 мл весит как гарнир.
      Нужен вес категории «напитки» или отдельный учёт жидкости
- [ ] T061 В оригинальной тарелке есть полезные масла и вода отдельными
      элементами; сейчас масло уходит в `extra`, вода не учитывается вовсе
- [ ] T062 Оценка тарелки за неделю в `/stats`: сейчас доли видны только сразу
      после приёма пищи

## Приоритет 6 — web (`web/`)

- [BLOCKED: нужен реальный браузер/устройство с настоящим push-сервисом —
  в среде сборки нет способа проверить доставку end-to-end] T070 Сквозная
  проверка Web Push (`web/lib/webpush.php`): VAPID JWT и aes128gcm
  самопроверены юнит-тестом (подпись/шифрование расшифровываются обратно
  корректно), но реальная доставка через push-сервис браузера не прогонялась
- [ ] T071 Словарь в web (`web/app/js/dictionary.js`) пока не поддерживает
      виды `medication`/`symptom` — только `meal`/`item`/`product`; в боте
      они есть (`spec/dictionary.md`). Паритет tg/web (`CLAUDE.md` #11)
      требует либо реализовать, либо это осознанный `[WEB-ONLY/TG-ONLY]`
      разрыв — сейчас просто временно не сделано
